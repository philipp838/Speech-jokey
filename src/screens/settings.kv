#:kivy 2.3.0

<Settings>:
    md_bg_color: self.theme_cls.secondaryContainerColor

    MDBoxLayout:
        orientation: "vertical"
        MDTopAppBar:
            type: "small"
            pos_hint: {"center_x": .5, "top": 1}
            MDTopAppBarLeadingButtonContainer:
                spacing: "4dp"
                padding: 40,0,0,5
                MDActionTopAppBarButton:
                    id: btn_back
                    icon: "arrow-left"
                    height: "56dp"
                    on_release:
                        root.manager.transition.direction = 'right'
                        root.manager.current = "main"
            MDTopAppBarTitle:
                text: root.title
                pos_hint: {"center_x": .5, "center_y": .5}
        MDDivider:
            size_hint_x: 1
            pos_hint: {"center_x": .5, "center_y": .5}
        MDStackLayout:
            id: settings_container
            orientation: "tb-lr"
            padding: 40,20,0,0
            spacing: "4dp"
            MDButton:
                style: "text"
                pos_hint: {"x": .01}
                height: "56dp"
                MDButtonIcon:
                    icon: "chevron-right"
                    pos_hint: {"x": 0.08, "center_y": 0.5}
                MDButtonText:
                    text: "Length of breaks"
                # on_release: root.on_load_file()       andere funktion!